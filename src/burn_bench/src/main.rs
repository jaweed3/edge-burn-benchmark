//! Burn inference benchmark.
//!
//! Loads MobileNetV2 via burn-import ONNX → Burn graph,
//! runs 1000 warm + 1000 measured iterations, collects
//! latency distribution, and outputs JSON results.
//!
//! Usage:
//!   cargo run --release [-- --threads 1] [--warmup 200] [--measured 1000]

use std::fs;
use std::path::PathBuf;
use std::time::Instant;

use burn::tensor::{backend::Backend, Tensor};
use burn_ndarray::NdArrayBackend;

// The generated model module from build.rs -> ONNX import.
// We include the module generated by burn-import.
mod model {
    include!(concat!(env!("OUT_DIR"), "/model_mobilenetv2_7.rs"));
}

type B = NdArrayBackend<f32>;

const INPUT_SIZE: usize = 224;
const IMAGENET_CLASSES: usize = 1000;

// ---------------------------------------------------------------------------
// CLI args
// ---------------------------------------------------------------------------

struct Args {
    warmup: usize,
    measured: usize,
    threads: usize,
    output: PathBuf,
    model_path: PathBuf,
}

fn parse_args() -> Args {
    let mut warmup = 200usize;
    let mut measured = 1000usize;
    let mut threads = 1usize;
    let mut output = PathBuf::from("../../results");
    let mut model_path = PathBuf::from("../../models/mobilenetv2-7.onnx");

    let mut i = 1;
    let raw: Vec<String> = std::env::args().collect();
    while i < raw.len() {
        match raw[i].as_str() {
            "--warmup" => {
                i += 1;
                warmup = raw[i].parse().expect("--warmup N");
            }
            "--measured" => {
                i += 1;
                measured = raw[i].parse().expect("--measured N");
            }
            "--threads" => {
                i += 1;
                threads = raw[i].parse().expect("--threads N");
            }
            "--output" => {
                i += 1;
                output = PathBuf::from(&raw[i]);
            }
            "--model" => {
                i += 1;
                model_path = PathBuf::from(&raw[i]);
            }
            _ => {}
        }
        i += 1;
    }
    Args { warmup, measured, threads, output, model_path }
}

// ---------------------------------------------------------------------------
// Timing helpers
// ---------------------------------------------------------------------------

fn now_ms() -> f64 {
    Instant::now()
        .elapsed()
        .as_secs_f64()
        * 1000.0
}

// ---------------------------------------------------------------------------
// CPU / temp sampling (via /proc)
// ---------------------------------------------------------------------------

fn read_cpu_usage() -> f64 {
    let stat = fs::read_to_string("/proc/stat")
        .ok()
        .and_then(|s| {
            s.lines().next().map(|l| {
                let parts: Vec<f64> = l
                    .split_whitespace()
                    .skip(1)
                    .filter_map(|v| v.parse::<f64>().ok())
                    .collect();
                let total: f64 = parts.iter().sum();
                let idle = parts.get(3).copied().unwrap_or(0.0);
                if total > 0.0 {
                    (1.0 - idle / total) * 100.0
                } else {
                    0.0
                }
            })
        })
        .unwrap_or(0.0);
    cpu_usage
}

fn read_temperature() -> f64 {
    fs::read_to_string("/sys/class/thermal/thermal_zone0/temp")
        .ok()
        .and_then(|s| s.trim().parse::<f64>().ok())
        .map(|v| v / 1000.0)
        .unwrap_or(0.0)
}

fn read_memory_mb() -> f64 {
    fs::read_to_string("/proc/self/status")
        .ok()
        .and_then(|s| {
            s.lines()
                .find(|l| l.starts_with("VmRSS:"))
                .and_then(|l| {
                    l.split_whitespace()
                        .nth(1)
                        .and_then(|v| v.parse::<f64>().ok())
                })
        })
        .unwrap_or(0.0)
}

// ---------------------------------------------------------------------------
// Main benchmark
// ---------------------------------------------------------------------------

fn main() {
    let args = parse_args();

    println!("============================================================");
    println!("  Burn Benchmark — {} thread(s)", args.threads);
    println!("============================================================");
    println!("  Model:  {}", args.model_path.display());
    println!("  Warmup: {}  Measured: {}", args.warmup, args.measured);

    // Set thread pool size for NdArray backend
    std::env::set_var("OMP_NUM_THREADS", args.threads.to_string());
    std::env::set_var("RAYON_NUM_THREADS", args.threads.to_string());

    // Load the ONNX-generated model
    let graph = burn_import::onnx::ONNXGraph::new(args.model_path.to_str().unwrap())
        .expect("Failed to load ONNX model");
    let model = graph.register().expect("Failed to register ONNX model");

    // Pre-allocate input tensor (random data)
    let input: Tensor<B, 4> = Tensor::random(
        [1, 3, INPUT_SIZE, INPUT_SIZE],
        burn::tensor::Distribution::Uniform(0.0, 1.0),
    );

    // Container for latencies
    let mut latencies_ms: Vec<f64> = Vec::with_capacity(args.measured);
    let mut cpu_samples: Vec<f64> = Vec::new();
    let mut mem_samples: Vec<f64> = Vec::new();
    let mut temp_samples: Vec<f64> = Vec::new();

    // ---- Warmup ----
    println!("  Warmup: {} iterations...", args.warmup);
    for _ in 0..args.warmup {
        let _output: Tensor<B, 2> = model.forward(input.clone());
    }

    // ---- Measured ----
    println!("  Measuring: {} iterations (sampling CPU/mem/temp)...", args.measured);
    let start_total = Instant::now();

    for i in 0..args.measured {
        let t0 = now_ms();
        let _output: Tensor<B, 2> = model.forward(input.clone());
        let elapsed = now_ms() - t0;

        latencies_ms.push(elapsed);
        cpu_samples.push(read_cpu_usage());
        mem_samples.push(read_memory_mb());
        temp_samples.push(read_temperature());

        if (i + 1) % 200 == 0 {
            print!("\r    {}/{} done", i + 1, args.measured);
            use std::io::Write;
            std::io::stdout().flush().unwrap();
        }
    }
    let total_elapsed_s = start_total.elapsed().as_secs_f64();
    println!("\n    {} / {} done", args.measured, args.measured);

    // ---- Statistics ----
    latencies_ms.sort_by(|a, b| a.partial_cmp(b).unwrap());

    let n = latencies_ms.len();
    let mean = latencies_ms.iter().sum::<f64>() / n as f64;
    let median = latencies_ms[n / 2];
    let min = latencies_ms[0];
    let max = latencies_ms[n - 1];
    let variance = latencies_ms.iter().map(|v| (v - mean).powi(2)).sum::<f64>() / n as f64;
    let std_dev = variance.sqrt();

    // Throughput
    let throughput_fps = args.measured as f64 / total_elapsed_s;

    // Bootstrapped 95% CI
    let ci = bootstrap_ci(&latencies_ms, 10_000, 0.95, 42);

    // Outlier removal (IQR)
    let q1 = latencies_ms[n / 4];
    let q3 = latencies_ms[(3 * n) / 4];
    let iqr = q3 - q1;
    let lower = q1 - 1.5 * iqr;
    let upper = q3 + 1.5 * iqr;
    let clean: Vec<f64> = latencies_ms
        .iter()
        .copied()
        .filter(|v| *v >= lower && *v <= upper)
        .collect();
    let n_outliers = n - clean.len();
    let clean_mean = if !clean.is_empty() {
        clean.iter().sum::<f64>() / clean.len() as f64
    } else {
        mean
    };

    // CPU, memory, temp
    let cpu_mean = if !cpu_samples.is_empty() {
        cpu_samples.iter().sum::<f64>() / cpu_samples.len() as f64
    } else {
        0.0
    };
    let mem_peak = mem_samples.iter().cloned().fold(0.0_f64, f64::max);
    let mem_mean = if !mem_samples.is_empty() {
        mem_samples.iter().sum::<f64>() / mem_samples.len() as f64
    } else {
        0.0
    };
    let temp_mean = if !temp_samples.is_empty() {
        temp_samples.iter().sum::<f64>() / temp_samples.len() as f64
    } else {
        0.0
    };
    let temp_peak = temp_samples.iter().cloned().fold(0.0_f64, f64::max);

    // ---- Print ----
    println!("\n  --- Results ({}t) ---", args.threads);
    println!("  Latency:     {:.2} ± {:.2} ms (median {:.2})", mean, std_dev, median);
    println!("  95% CI:      [{:.2}, {:.2}] ms", ci.0, ci.1);
    println!("  Throughput:  {:.1} fps", throughput_fps);
    println!("  CPU:         {:.1}%", cpu_mean);
    println!("  Memory:      {:.0} MB peak ({:.0} MB mean)", mem_peak, mem_mean);
    println!("  Temp:        {:.1}°C mean ({:.1}°C peak)", temp_mean, temp_peak);
    println!("  Outliers:    {} / {} removed", n_outliers, n);
    println!("  Valid:       {} samples", clean.len());

    // ---- Save JSON ----
    let machine_info = serde_json::json!({
        "hostname": std::env::var("HOSTNAME").unwrap_or_default(),
        "machine": "aarch64",
        "cpu_count": 4,
        "rust_version": env!("CARGO_PKG_RUST_VERSION"),
    });

    let result = serde_json::json!({
        "config": {
            "framework": "burn",
            "model": "mobilenetv2-7.onnx",
            "input_shape": [1, 3, 224, 224],
            "n_warmup": args.warmup,
            "n_measured": args.measured,
            "n_threads": args.threads,
        },
        "summary": {
            "latency_mean_ms": (mean * 100.0).round() / 100.0,
            "latency_median_ms": (median * 100.0).round() / 100.0,
            "latency_std_ms": (std_dev * 100.0).round() / 100.0,
            "latency_min_ms": (min * 100.0).round() / 100.0,
            "latency_max_ms": (max * 100.0).round() / 100.0,
            "throughput_fps": (throughput_fps * 100.0).round() / 100.0,
            "ci_95_lower_ms": (ci.0 * 100.0).round() / 100.0,
            "ci_95_upper_ms": (ci.1 * 100.0).round() / 100.0,
            "cpu_mean_pct": (cpu_mean * 10.0).round() / 10.0,
            "memory_peak_mb": (mem_peak * 10.0).round() / 10.0,
            "memory_mean_mb": (mem_mean * 10.0).round() / 10.0,
            "temp_mean_c": (temp_mean * 10.0).round() / 10.0,
            "temp_peak_c": (temp_peak * 10.0).round() / 10.0,
            "n_valid": clean.len(),
            "n_outliers_removed": n_outliers,
        },
        "machine_info": machine_info,
    });

    fs::create_dir_all(&args.output).expect("Failed to create output dir");
    let out_path = args.output.join(format!("burn_{}t.json", args.threads));
    fs::write(&out_path, serde_json::to_string_pretty(&result).unwrap())
        .expect("Failed to write results");
    println!("  Results saved to {}", out_path.display());

    // Machine info sidecar
    let info_path = args.output.join(format!("burn_{}t_machine.json", args.threads));
    fs::write(&info_path, serde_json::to_string_pretty(&machine_info).unwrap())
        .expect("Failed to write machine info");
}

// ---------------------------------------------------------------------------
// Bootstrapped confidence interval
// ---------------------------------------------------------------------------

fn bootstrap_ci(data: &[f64], n_bootstrap: usize, ci: f64, seed: u64) -> (f64, f64) {
    use rand::Rng;
    let mut rng: rand::rngs::StdRng = rand::SeedableRng::seed_from_u64(seed);

    let n = data.len();
    if n < 2 {
        return (data[0], data[0]);
    }

    let mut means = Vec::with_capacity(n_bootstrap);
    for _ in 0..n_bootstrap {
        let mut sum = 0.0_f64;
        for _ in 0..n {
            let idx = rng.gen_range(0..n);
            sum += data[idx];
        }
        means.push(sum / n as f64);
    }

    means.sort_by(|a, b| a.partial_cmp(b).unwrap());
    let alpha = (1.0 - ci) / 2.0;
    let lo_idx = (alpha * n_bootstrap as f64) as usize;
    let hi_idx = ((1.0 - alpha) * n_bootstrap as f64) as usize;
    (means[lo_idx], means[hi_idx.min(n_bootstrap - 1)])
}
